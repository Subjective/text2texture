# Environment variables for the Demo Backend

# --- OpenAI API Key (Required for Text-to-Image) ---
# Get your key from https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# --- SAM 2 Model Paths (Required for Point-based Mask Prediction) ---
# Adjust paths relative to the demo/backend directory or use absolute paths
SAM2_CONFIG_PATH="../sam2/sam2/configs/sam2.1/sam2.1_hiera_l.yaml"
SAM2_CHECKPOINT_PATH="../../checkpoints/sam2.1_hiera_large.pt"

# --- SAM-HQ Model Paths (Required for Automatic Mask Generation) ---
# Adjust paths relative to the demo/backend directory or use absolute paths
SAM_HQ_MODEL_TYPE="vit_h" # Or vit_l, vit_b, etc. - MUST match the checkpoint file
SAM_HQ_CHECKPOINT_PATH="../../checkpoints/sam_hq_vit_h.pth"

# --- Florence-2 Model ID (Optional) ---
# Default is "microsoft/Florence-2-large-ft"
# FLORENCE2_MODEL_ID="microsoft/Florence-2-large-ft"

# --- Flask Server Configuration (Optional) ---
# FLASK_HOST="127.0.0.1"
# FLASK_PORT="5000"
# FLASK_DEBUG="True" # Set to "False" for production-like behavior